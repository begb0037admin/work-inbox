I have connected my Oxford University Microsoft 365 account (kevin.lelitte@admin.ox.ac.uk) to you via the Outlook/Teams/Calendar connectors. Before building anything, do the following and report back exactly what you find — do not assume any capability, verify it live:

1. Verify live connector scope. Confirm what you can currently do against this account: read inbox (which folders?), read sent items, read calendar (date range supported, recurrence expansion behaviour), read Teams chats/channels. State explicitly which of these are read-only vs. which also expose write actions (send, draft, move, delete, create event, post message) — I need to know what's technically possible even though nothing gets written without my separate explicit approval later.
2. Identify the exact ID format you get back for: an email message (is it a Graph id, an internetMessageId, both?); a calendar event (Graph id, iCalUId, or both?); a Teams message (its "canonical path" or equivalent). Report the literal field names and a sample value for each (redact the actual content, just show shape/format).
3. Do a bounded read-only pull and return it as JSON, not prose: Inbox — newest 50 messages (subject, sender name + SMTP address, received datetime, unread flag, folder name, the message ID from step 2, a short body snippet). Sent items — newest 20, same fields. Calendar — today through 6 days forward, and 30 days back (organizer, attendees, subject, start/end with time zone, the event ID from step 2, recurrence flag). Teams — list chats you can see (topic, last-updated, member count); do not pull message bodies yet, just the chat list.
4. Do not write anything — no drafts, no sends, no calendar changes, no folder moves. Read-only pull only.
5. Return: your answers to 1 and 2 as plain text; the JSON from step 3 as a single fenced code block; anything that failed or was denied and why.
